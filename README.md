# Tharun Portfolio (Deployment Contents)

This folder contains the production-ready build of the portfolio. Use the contents of this folder to deploy to GitHub Pages or any static hosting provider.

Location: `tharun-portfolio/dist/`

Quick deploy steps:

```powershell
cd tharun-portfolio/dist
git init
git remote add origin https://github.com/THARUN-GOWDA-K/THARUN-GOWDA-K.github.io.git
git add .
git commit -m "Deploy: updated portfolio"
git push origin main --force
```

If you'd like, I can push these files for you from this workspace. Tell me which repository you want as the deployment target.
